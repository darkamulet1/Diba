# Architecture
## Vectorized Engine
Uses `VectorizedProvider` for batch processing via numpy arrays. `VectorizedAdapter` maps these back to `SkyFrame` objects.
**Note:** This is a mock implementation for demonstration. It does not use external C-extensions.

